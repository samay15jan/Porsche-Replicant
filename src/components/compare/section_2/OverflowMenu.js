import React from 'react'
import tw from 'twin.macro'
import styled from 'styled-components/macro'

const Container = styled.div`${tw`overflow-hidden`}`
const OverflowMenu = () => {
    return (
        <Container>

        </Container>
    )
}

export default OverflowMenu